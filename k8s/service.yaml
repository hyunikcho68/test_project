apiVersion: v1
kind: Service
metadata:
  name: monitoring-dashboard
spec:
  selector:
    app: monitoring-dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
